diff a/WinFormsApp1/Form1.cs b/WinFormsApp1/Form1.cs	(rejected hunks)
@@ -10,6 +10,24 @@
         public Form1()
         {
             InitializeComponent();
+            UpdateTimerDisplay(); 
+        }
+
+        private void UpdateTimerDisplay()
+        {
+            int minutes = _remainingSeconds / SecondsPerMinute;
+            int seconds = _remainingSeconds % SecondsPerMinute;
+            labelTime.Text = $"{minutes:00}:{seconds:00}";
+        }
+
+        private void StopTimer()
+        {
+            if (_isRunning)
+            {
+                timer1.Stop();
+                _isRunning = false;
+                buttonStartStop.Text = "�X�^�[�g";
+            }
         }
 
         private void buttonStartStop_Click(object sender, EventArgs e)
@@ -21,8 +39,7 @@
             }
             else
             {
-                timer1.Stop();
-                buttonStartStop.Text = "�X�^�[�g";
+                StopTimer();
             }
             _isRunning = !_isRunning;
         }
@@ -32,14 +49,18 @@
             _remainingSeconds--;
             if (_remainingSeconds < 0)
             {
-                timer1.Stop();
+                StopTimer();
                 MessageBox.Show("���ԂɂȂ�܂����I");
-                _remainingSeconds = WorkMinutes * SecondsPerMinute;  // ���Z�b�g
+                _remainingSeconds = WorkMinutes * SecondsPerMinute; 
             }
+            UpdateTimerDisplay();
+        }
 
-            int minutes = _remainingSeconds / SecondsPerMinute;
-            int seconds = _remainingSeconds % SecondsPerMinute;
-            labelTime.Text = $"{minutes:00}:{seconds:00}";
+        private void resetButton_Click(object sender, EventArgs e)
+        {
+            StopTimer();
+            _remainingSeconds = WorkMinutes * SecondsPerMinute;
+            UpdateTimerDisplay();
         }
     }
-}
+}
\ No newline at end of file
